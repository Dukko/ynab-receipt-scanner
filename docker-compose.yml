services:
  app:
    image: ghcr.io/dukko/ynab-receipt-scanner:latest
    build: .
    ports:
      - "3000:3000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - YNAB_ACCESS_TOKEN=${YNAB_ACCESS_TOKEN}
      - PORT=3000
    restart: unless-stopped
